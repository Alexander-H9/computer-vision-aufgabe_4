# Detect QR-Codes from video capture

with 'e' and 'd', you can change the erode filter \
with 'd' and 'f' you can change the dilate filter \
\
use both filters to get the optimal output for the code detection